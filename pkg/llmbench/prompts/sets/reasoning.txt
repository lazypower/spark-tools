A company has 5 servers, each with 99.9% uptime individually. They are configured in an active-active setup where the system is considered operational as long as at least 3 of the 5 servers are running. Calculate the overall system availability. Show your work step by step, including the binomial probability calculations for exactly 3, 4, and 5 servers being up, and express the final answer as a percentage with 6 decimal places.
---
You have a dataset of 10 million records that needs to be sorted. The data is stored on disk because it doesn't fit in memory (you have 1 GB of RAM available). Describe the optimal sorting strategy step by step. Calculate the number of merge passes needed, the total disk I/O in bytes, and estimate the total time assuming 100 MB/s sequential read/write speed and 10ms random access latency. Compare external merge sort with replacement selection.
---
A web application receives 10,000 requests per second on average, with each request taking 50ms to process. The requests follow a Poisson distribution. Using queuing theory (M/M/c model), determine: (1) The minimum number of server threads needed to keep the system stable. (2) The average wait time with that minimum number of threads. (3) How many threads are needed to keep the average wait time below 10ms. Show all calculations using Little's Law and the Erlang C formula.
---
Design an algorithm to find the shortest path in a graph where edges have time-dependent weights (the travel time on an edge depends on when you start traversing it). The graph has 100,000 nodes and 500,000 edges. Analyze why standard Dijkstra's algorithm fails for this case, propose a modified algorithm that handles time-dependent weights correctly, prove its correctness, and analyze its time complexity. Discuss optimizations like A* heuristics and time-expanded graphs.
---
You are designing a distributed consensus system that must handle Byzantine faults. Explain the Byzantine Generals Problem, prove why 3f+1 nodes are needed to tolerate f Byzantine faults, and walk through the PBFT algorithm step by step for a scenario with 4 nodes where one node is Byzantine. Show the message exchange pattern for all three phases (pre-prepare, prepare, commit), including the exact messages sent by the faulty node and how the honest nodes detect and tolerate the fault.
---
A database table has 100 million rows with a composite index on columns (A, B, C). For each of the following queries, explain whether the index will be used and how efficiently, describing the B-tree traversal path: (1) WHERE A = 1 AND B > 5 AND C = 3, (2) WHERE B = 2 AND C = 3, (3) WHERE A = 1 AND C = 3, (4) WHERE A IN (1,2,3) AND B BETWEEN 10 AND 20, (5) SELECT A, B, C WHERE A = 1 ORDER BY B DESC LIMIT 10. For each case, estimate the number of disk pages accessed assuming 100 rows per leaf page.
---
Analyze the time and space complexity of the following algorithm. Then optimize it to run in O(n) time:

```
def count_inversions(arr):
    count = 0
    for i in range(len(arr)):
        for j in range(i+1, len(arr)):
            if arr[i] > arr[j]:
                count += 1
    return count
```

Provide the optimized algorithm using a modified merge sort approach. Prove that your optimized version produces the same result as the brute force approach. Walk through a concrete example with the array [5, 3, 8, 1, 2].
---
A system processes events from 1000 IoT sensors, each sending data every second. Events must be processed in order per sensor but can be processed in parallel across sensors. Design a partitioning and processing strategy that: (1) Guarantees per-sensor ordering with exactly-once semantics. (2) Scales horizontally across multiple consumer instances. (3) Handles consumer failures without message loss. (4) Handles a consumer falling behind (backpressure). Analyze the throughput limits of your design. Calculate the maximum end-to-end latency under normal load and under a failure scenario where one of four consumers crashes and its partitions are rebalanced.
---
Prove that the halting problem is undecidable using a diagonalization argument. Then explain the practical implications for software verification: why can't we build a perfect static analyzer that detects all bugs? Describe three specific categories of program properties that are undecidable (Rice's theorem), and for each, explain what approximations real-world tools use and what tradeoffs those approximations make (false positives vs false negatives). Give concrete examples of bugs that escape analysis and bugs that are falsely flagged.
---
You need to design a cache with the following constraints: 1 million items maximum, 95th percentile lookup time under 1 microsecond, items expire after a TTL that varies per item, and memory usage must stay under 500 MB. Compare four cache eviction strategies (LRU, LFU, ARC, W-TinyLFU) for this workload. For each strategy, analyze: memory overhead per entry, lookup time complexity, hit rate for a Zipfian(0.99) access pattern, and how TTL expiration interacts with the eviction policy. Calculate the theoretical maximum hit rate given the cache size and access pattern. Recommend the best strategy and justify your choice with quantitative arguments.
